diff --git a/Cargo.toml b/Cargo.toml
index 3eb7321..0cb2201 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -3,6 +3,9 @@ name = "bingee-desktop"
 version = "0.1.0"
 edition = "2024"
 
+[features]
+r4-measurement = []
+
 [dependencies]
 rusqlite = { version = "0.40.2", features = ["bundled"] }
 slint = "1.17.1"
diff --git a/src/library.rs b/src/library.rs
index a1458bd..1ad3edb 100644
--- a/src/library.rs
+++ b/src/library.rs
@@ -267,7 +267,7 @@ fn generated_record(k: usize) -> RecordFields {
 ///
 /// The R1 in-memory search. Since R2 the app searches with `db::search`; this
 /// stays as the reference oracle its tests compare against.
-#[cfg(test)]
+#[cfg(any(test, feature = "r4-measurement"))]
 pub fn search(items: &[MediaItem], query: &str) -> Vec<usize> {
     let query = query.trim().to_lowercase();
     items
diff --git a/src/main.rs b/src/main.rs
index c9c18cb..8be39e4 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -6,6 +6,10 @@ mod db;
 mod library;
 mod poster;
 
+#[cfg(feature = "r4-measurement")]
+#[path = "../benchmark/r4_latency.rs"]
+mod r4_latency;
+
 use std::cell::{Cell, RefCell};
 use std::path::{Path, PathBuf};
 use std::rc::Rc;
@@ -18,6 +22,14 @@ use slint::{Color, Image, Model, ModelNotify, ModelRc, ModelTracker};
 slint::include_modules!();
 
 fn main() -> Result<(), slint::PlatformError> {
+    #[cfg(feature = "r4-measurement")]
+    if std::env::args().nth(1).as_deref() == Some("--r4-latency") {
+        if let Err(error) = r4_latency::run() {
+            eprintln!("R4 measurement failed: {error}");
+            std::process::exit(1);
+        }
+        return Ok(());
+    }
     let window = AppWindow::new()?;
     match open_library() {
         Ok(view) => connect(&window, Rc::new(view)),
