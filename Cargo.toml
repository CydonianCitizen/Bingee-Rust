[package]
name = "bingee-desktop"
version = "0.1.0"
edition = "2024"

[features]
r4-measurement = []

[dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }
slint = "1.17.1"

[build-dependencies]
slint-build = "1.17.1"

[dev-dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg"] }
